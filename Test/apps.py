from django.apps import AppConfig


class TestConfig(AppConfig):
    name = 'Test'

app.listen(process.env.PORT or 8080, function(){

    console.log("Express server listening on port %d in %s mode", this.address().port, app.settings.env);

})